// Write a function to reverse the elements of an integer array in place. For example, 
// if the array is [1, 2, 3, 4, 5], after reversing it should be [5, 4, 3, 2, 1].

#include<stdio.h>
int main(){
    int n;
    int arr[n] = {1,2,3,4,5};
    int length = sizeof(arr[n]);
    int i;
    for(length = n; length > n; length--){
   
        printf("%d", arr[i]);
    }

}
